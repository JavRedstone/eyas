# HF Spaces Gradio SDK reads this file at build time.
# gradio, spaces, torch==2.11.0, huggingface-hub, datasets are pre-installed
# in the HF base image; list only what the base image does not provide.
llama-cpp-python
opencv-python
numpy
pillow
torchaudio
transformers
ultralytics
voxcpm
# nano-vllm-voxcpm is intentionally omitted: its flash-attn dep requires torch
# at compile time and breaks pip on CPU-only build machines (including HF Spaces).
# On a bare CUDA server: pip install nano-vllm-voxcpm (set EYAS_ZERO_GPU=0)
python-dotenv>=1.0
