"""Video processing utilities: split clips, run multimodal model, extract clip annotations."""

from typing import List, Dict


def process_clip(path: str) -> List[Dict]:
    """Process a short video clip and return annotations (actions, interactions).
    """
    # TODO: call the MiniCPM-V wrapper and return structured annotations
    return []
